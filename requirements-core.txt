# BlindSpot CORE requirements (Tiers 1 & 2).
# Install into a Python 3.11 venv. This alone gives you:
#   - Tier 1 narrator (camera -> YOLO -> voice)
#   - Tier 2 VLM client (stub / gradio / openai modes)
#
# GPU NOTE (RTX 3050): install the CUDA build of torch FIRST, separately, so YOLO
# uses the GPU. See README "Step 2". If you skip it you get CPU torch (slower but
# still works). Do NOT let ultralytics pull a plain torch before you do this.

# --- vision ---
ultralytics==8.3.0            # YOLOv8 (pulls opencv, numpy, etc.)
opencv-python==4.10.0.84

# --- text-to-speech (neural, needs internet) ---
edge-tts>=7.0.0
playsound==1.2.2             # simple mp3 playback
pyttsx3==2.90               # offline fallback voice

# --- VLM client ---
gradio-client==1.3.0         # for VLM_MODE="gradio"
requests>=2.31.0             # for VLM_MODE="openai"
pillow>=10.0.0
numpy>=1.24,<2.0             # keep <2.0 for broad ML-lib compatibility
